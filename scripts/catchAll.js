/*
let turing = require('./TuringAPI.js');
let Turing = turing.Turing;
turing = null;

module.exports = (robot) => {
	
	robot.catchAll(
		(res) => {
			// res.reply(Turing);
			Turing(robot, res);
			// return;
		}
		)
}*/

/*module.exports = (robot) ->
  robot.catchAll (res) ->
    res.reply "#{res.message.text}"*/